let comments = document.getElementById("comment-list");

comments.addEventListener("click", function (ev) {
    ev.target.parentNode.parentNode;
});

comments.addEventListener("keydown", function (ev) {

});
